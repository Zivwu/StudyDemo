<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gossip.android.view">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".TestListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".widget.wave.WaveActivity" />
        <activity android:name=".widget.heat.HeartActivity"></activity>
        <activity android:name=".widget.paint.shader.ShaderActivity" />
        <activity android:name=".path.PathActivity" />
        <activity android:name=".gossip.pk.PkActivity" />
    </application>

</manifest>